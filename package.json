{
  "scripts": {
    "test": "npm run lint && npm run test:unit && npm run test:api",
    "lint": "eslint src/",
    "test:unit": "DATASTORE_NAMESPACE=test nyc --reporter=lcov --reporter=text mocha --reporter mochawesome --reporter-options reportDir=testresults,reportFilename=index.html --timeout=60000 ./src/*.test.js",
    "test:api": "./run-api-tests.sh"
  },
  "devDependencies": {
    "@google-cloud/functions-emulator": "^1.0.0-alpha.25",
    "casual": "^1.5.17",
    "chai": "^4.1.2",
    "eslint": "^4.8.0",
    "mocha": "^3.5.3",
    "mocha-logger": "^1.0.5",
    "mochawesome": "^2.3.1",
    "newman": "^3.8.2",
    "nyc": "^11.2.1"
  },
  "dependencies": {
    "@google-cloud/datastore": "^1.1.0",
    "bcrypt": "^1.0.3",
    "jwt-simple": "^0.5.1",
    "slug": "^0.9.1"
  }
}
