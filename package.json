{
  "scripts": {
    "test": "npm run test:unit && npm run test:api",
    "test:unit": "DATASTORE_NAMESPACE=test MOCHA_FILE=./test-results.xml nyc --reporter=lcov --reporter=text mocha --reporter mocha-junit-reporter --reporter spec --timeout=60000 ./src/*.test.js",
    "test:api": "./run-api-tests.sh"
  },
  "devDependencies": {
    "@google-cloud/functions-emulator": "^1.0.0-alpha.25",
    "casual": "^1.5.17",
    "chai": "^4.1.2",
    "mocha": "^3.5.3",
    "mocha-junit-reporter": "^1.15.0",
    "mocha-logger": "^1.0.5",
    "newman": "^3.8.2",
    "nyc": "^11.2.1"
  },
  "dependencies": {
    "@google-cloud/datastore": "^1.1.0",
    "bcrypt": "^1.0.3",
    "jwt-simple": "^0.5.1",
    "slug": "^0.9.1"
  }
}
